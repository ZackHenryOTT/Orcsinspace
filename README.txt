Dead-Zone Ops static restore package.

Root site: restored full app shell with squad builder, missions, logs, save/reset, and campaign loop from the last good source-build artifact.

Extra route: /multiclient-foundation/ contains the separate player/DM prototype work so it no longer replaces the main game shell.
