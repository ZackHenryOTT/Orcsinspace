This folder is a standalone structural slice for the real product direction:
- separate per-player screens
- separate DM view
- local multi-client test harness
- mapped tutorial level grammar

It is included directly in the source package and the drag-drop deploy package so the artifact workflow stays consistent while the main React/Vite app catches up to this architecture.
